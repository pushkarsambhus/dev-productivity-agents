# Install: uv pip install -r requirements.txt
#
# Core dependencies — Anthropic and OpenAI Python SDKs
anthropic>=0.40.0
openai>=1.0.0

# Testing
pytest>=8.0.0
pytest-mock>=3.12.0

# Optional: timezone support for get_current_time tool
# pytz>=2024.1

# Optional: richer HTTP requests (not needed for the built-in fetch_url tool)
# requests>=2.32.0
